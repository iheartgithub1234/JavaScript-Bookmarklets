javascript:(function() {  var cursor = prompt("Enter the cursor style you want to use:\n\nPossible values: default, pointer, crosshair, move, text, invisible, help");  if (!cursor) {    alert("You didn't enter a cursor style.");    return;  }  var cursorStyles = ["default", "pointer", "crosshair", "move", "text", "invisible", "help"];  if (cursorStyles.indexOf(cursor) === -1) {    alert("Invalid cursor style. Please choose from the following: " + cursorStyles.join(", "));    return;  }  if (cursor === "invisible") {    document.body.style.cursor = "none";  } else {    document.body.style.cursor = cursor;  }})();
